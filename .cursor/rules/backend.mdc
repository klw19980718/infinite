---
alwaysApply: true
---

## Supabase 用法
- 仅通过 `@/lib/supabase` 暴露的 helper 使用：
  - `getSupabaseClient()` - 用于 Client 组件（浏览器环境）
  - `getServerSupabase()` - 用于服务端，需要 SERVICE_ROLE_KEY 的操作（如 webhooks）
- 服务端认证（从 cookie 读取 session）：
  - `@/lib/supabase/server.server.ts` - 服务端专用文件（`.server.ts` 后缀确保不会被打包到客户端）
    - `createServerSupabaseClient()` - 用于 Server Components 和 Server Actions
    - `createServerSupabaseClientForRouteHandler(request, response)` - 用于 API Route Handlers
- 所有查询默认 **RLS 开启**；服务端需要更高权限时，使用**服务端密钥**于 Route Handler（不可在客户端暴露）。

## API & Route Handlers
- API 路由放 /app/api/**，默认 Edge 仅当无 Node 依赖。
- 校验：Zod（或最小手写校验）；返回明确的错误结构 `{ ok:false, code, message }`。
- 禁止在 API 中做 SSR-only 任务与重量级阻塞（交给队列/edge/cron）。
⚠️ **重要维护提醒**：
- 🔴 当项目app/api有任何改动时，必须立即更新此文件
- 🔴 保持此内容为最新

## 音频处理工具
- **音频时长计算**: 使用 `@/lib/audio-utils` 中的 `getAudioDuration()` 函数
- **实现方式**: 基于 `music-metadata` 库，从音频文件 Buffer 中解析元数据获取时长
- **支持格式**: MP3, WAV, M4A, OGG, FLAC 等常见音频格式
- **使用场景**: 视频生成 API（image-to-video, video-to-video）中，后端自动计算音频时长用于积分计算
- **注意**: 前端不再需要传递 `audio_duration` 字段，后端会从上传的音频文件中直接读取

### 🚀 Creem 支付集成

#### POST `/api/creem/checkout`
**功能**: 创建 Creem 支付会话，返回支付页面 URL  
**请求体**:
```typescript
{
  planId: 'pro' | 'ultimate' | 'enterprise'
}
```
**返回**:
```typescript
{
  ok: true,
  checkout_url: string,
  checkout_id: string,
  request_id: string
}
// 错误情况：
{
  ok: false,
  code: 'INVALID_PLAN' | 'UNAUTHORIZED' | 'PRODUCT_NOT_FOUND' | 'CHECKOUT_ERROR' | 'INTERNAL_ERROR',
  message: string
}
```
**流程**:
1. 从 cookie 验证用户身份（使用 `createServerSupabaseClientForRouteHandler`）
2. 验证 planId 并映射到 Creem 产品 ID
3. 生成唯一 request_id（基于用户ID）
4. 调用 Creem API 创建 checkout 会话（metadata 中自动包含服务端获取的 user.id 和 user.email）
5. 返回支付页面 URL，前端重定向

**认证方式**: 使用 cookie-based 认证，从 `NextRequest` 读取用户 session

**环境变量**: `CREEM_API_KEY`, `CREEM_PRODUCT_PRO_ID`, `CREEM_PRODUCT_ULTIMATE_ID`, `CREEM_PRODUCT_ENTERPRISE_ID`, `CREEM_API_URL` (可选), `NEXT_PUBLIC_SITE_URL`

#### POST `/api/creem/webhook`
**功能**: 处理 Creem 支付完成 webhook 事件，更新用户积分  
**请求头**: 
- `Creem-Signature` 或 `x-creem-signature` (HMAC 签名)
- `Creem-Timestamp` 或 `x-creem-timestamp` (可选，用于签名验证)
**请求体**: Creem webhook 事件 JSON
```typescript
{
  id: string,              // 事件唯一ID（用于幂等性）
  eventType: 'checkout.completed' | 'checkout.session.completed',
  object: {
    id: string,            // checkout.id
    request_id: string,
    metadata: {
      user_id: string,    // UUID
      user_email: string,
      plan_id: 'pro' | 'ultimate' | 'enterprise'
    },
    order?: {
      id: string,
      product: string,    // Creem 产品 ID
      amount: number,
      currency: string
    },
    product?: {
      id: string          // Creem 产品 ID
    }
  }
}
```
**返回**: 
```typescript
{ received: true }  // 成功或重复请求
// 错误情况：
{
  ok: false,
  message: string
}
```
**流程**:
1. 验证 HMAC 签名（生产环境强制，开发环境可选）
2. 幂等性检查（通过 `event.id` 和 `checkout.id`）
3. 从 metadata 提取 user_id 和 plan_id
4. 根据 plan_id 计算积分（pro: 400, ultimate: 800, enterprise: 1800）
5. 调用 `add_user_credits` RPC 函数增加积分
6. 映射 Creem 产品 ID 到本地 products 表 UUID
7. 创建 orders 记录（status='paid'）
8. 更新 payments 记录（关联 order_id 和完整 payload）

**幂等性策略**:
- 主锁：`payments.unique_key = event.id`，冲突则直接返回 200
- 次锁：`orders.creem_session_id = checkout.id`，已存在则跳过

**积分映射**: `{ pro: 400, ultimate: 800, enterprise: 1800 }`

**环境变量**: `CREEM_WEBHOOK_SECRET`

### 🎥 WaveSpeedAI 视频生成集成

#### POST `/api/video/image-to-video`
**功能**: 提交图片转视频任务，从图片和音频生成说话视频  
**请求方式**: `multipart/form-data`  
**请求参数**:
```typescript
{
  image: File (必需) - 输入图片文件
  audio: File (必需) - 输入音频文件
  resolution: 'fast' | '480p' | '720p' (可选, 默认: 'fast')
  prompt: string (可选) - 提示词
  seed: number (可选, 默认: -1) - 随机种子
  mask_image: File (可选) - 掩码图片，指定要动画化的区域
}
```
**返回**:
```typescript
{
  ok: true,
  task_id: string (uuid),
  wavespeed_task_id: string,
  status: 'processing'
}
// 错误情况：
{
  ok: false,
  code: 'UNAUTHORIZED' | 'MISSING_FILES' | 'INVALID_RESOLUTION' | 'INVALID_AUDIO' | 'AUDIO_TOO_LONG' | 'AUDIO_DURATION_ERROR' | 'TASK_CREATION_FAILED' | 'WAVESPEED_ERROR' | 'INTERNAL_ERROR',
  message: string
}
```
**流程**:
1. 从 cookie 验证用户身份（使用 `createServerSupabaseClientForRouteHandler`）
2. 验证必需文件（image, audio）
3. **后端计算音频时长**（使用 `@/lib/audio-utils` 的 `getAudioDuration` 函数，基于 `music-metadata` 库）
4. 验证音频时长有效性（必须 > 0）和最大时长限制（600秒）
5. **根据音频时长和 resolution 计算所需积分，先检查用户积分是否足够（在文件上传前）**
6. 上传文件到 WaveSpeedAI 媒体存储
7. 调用 `create_video_task` RPC 函数创建任务并扣除积分（传入后端计算的音频时长）
8. 构建 Webhook URL（包含 task_id）
9. **根据 resolution 选择 API 接口**：
   - `fast`: 调用 `/api/v3/wavespeed-ai/infinitetalk-fast`（不传 resolution 参数）
   - `480p` 或 `720p`: 调用 `/api/v3/wavespeed-ai/infinitetalk`（传递 resolution 参数）
10. 提交任务到 WaveSpeedAI API（携带 Webhook URL）
11. 更新任务状态为 'processing'
12. 返回任务 ID

**认证方式**: 使用 cookie-based 认证，从 `NextRequest` 读取用户 session

**音频时长计算**: 
- 后端使用 `music-metadata` 库从音频文件直接读取时长
- 支持格式：MP3, WAV, M4A, OGG, FLAC 等
- 如果无法读取时长，返回 `INVALID_AUDIO` 错误
- 如果时长超过 600 秒，返回 `AUDIO_TOO_LONG` 错误

**积分扣除**: 
- **fast**: 0.5 积分/秒（向上取整），最小 3 积分
- **480p**: 1 积分/秒，最小 5 积分
- **720p**: 2 积分/秒，最小 10 积分
- 最大: 600 秒（fast=300积分，480p=600积分，720p=1200积分）
- 基于后端计算的音频时长进行积分计算

**环境变量**: `WAVESPEED_KEY`, `WAVESPEED_WEBHOOK_SECRET`, `NGROK_DEV_URL` (开发环境)

#### POST `/api/video/video-to-video`
**功能**: 提交视频转视频任务，从视频和音频生成说话视频  
**请求方式**: `multipart/form-data`  
**请求参数**:
```typescript
{
  video: File (必需) - 输入视频文件
  audio: File (必需) - 输入音频文件
  resolution: 'fast' | '480p' | '720p' (可选, 默认: 'fast')
  prompt: string (可选) - 提示词
  seed: number (可选, 默认: -1) - 随机种子
  mask_image: File (可选) - 掩码图片，指定要动画化的区域
}
```
**返回**: 同 image-to-video 接口
**流程**: 
1. 同 image-to-video 接口（使用 video 文件而非 image）
2. **后端计算音频时长**（使用 `@/lib/audio-utils` 的 `getAudioDuration` 函数）
3. 验证音频时长有效性（必须 > 0）和最大时长限制（600秒）
4. **根据音频时长和 resolution 计算所需积分，先检查用户积分是否足够（在文件上传前）**
5. **根据 resolution 选择 API 接口**：
   - `fast`: 调用 `/api/v3/wavespeed-ai/infinitetalk-fast/video-to-video`（不传 resolution 参数）
   - `480p` 或 `720p`: 调用 `/api/v3/wavespeed-ai/infinitetalk/video-to-video`（传递 resolution 参数）
**认证方式**: 使用 cookie-based 认证，从 `NextRequest` 读取用户 session
**音频时长计算**: 同 image-to-video 接口
**积分扣除**: 同 image-to-video 接口

#### POST `/api/video/webhook`
**功能**: 接收 WaveSpeedAI 任务完成/失败回调，更新任务状态
**请求头**:
- `webhook-signature` (HMAC 签名)
- `webhook-id` (事件ID)
- `webhook-timestamp` (时间戳)
**请求体**: WaveSpeed Webhook JSON
```typescript
{
  id: string,
  status: 'completed' | 'failed',
  outputs: string[],
  error?: string
  // ...
}
```
**返回**: `200 OK`
**流程**:
1. 验证 HMAC 签名（使用 `WAVESPEED_WEBHOOK_SECRET`）
2. 幂等性检查（查询 `webhook_events` 表）
3. 提取 `task_id`（从 URL query 参数）
4. 调用 `update_video_task` RPC 更新任务状态和结果
5. 如果任务完成，设置 `expires_at`（7天保留期）
6. 记录 webhook 事件到 `webhook_events` 表

**环境变量**: `WAVESPEED_WEBHOOK_SECRET`

#### GET `/api/video/result`
**功能**: 查询视频生成任务结果  
**查询参数**:
```typescript
{
  task_id: string (uuid, 必需) - 任务 ID
}
```
**返回**:
```typescript
{
  ok: true,
  task_id: string,
  status: 'created' | 'processing' | 'completed' | 'failed',
  outputs: string[],  // 完成时包含视频 URL 数组
  error?: string     // 失败时的错误信息
}
// 错误情况：
{
  ok: false,
  code: 'UNAUTHORIZED' | 'MISSING_TASK_ID' | 'TASK_NOT_FOUND' | 'WAVESPEED_ERROR' | 'INTERNAL_ERROR',
  message: string
}
```
**流程**:
1. 从 cookie 验证用户身份（使用 `createServerSupabaseClientForRouteHandler`）
2. 验证任务归属（用户只能查询自己的任务）
3. 从 WaveSpeedAI API 获取任务状态
4. 更新本地任务状态和结果
5. **如果任务状态为 `completed`，自动设置 `expires_at = created_at + 7天`（用于作品7天保留期）**
6. 返回任务状态和结果

**认证方式**: 使用 cookie-based 认证，从 `NextRequest` 读取用户 session

**环境变量**: `WAVESPEED_KEY`

#### POST `/api/speech/text-to-speech`
**功能**: 使用 MiniMax Speech 02 Hd 将文本转换为语音（通过 WaveSpeedAI API）  
**请求方式**: `application/json`  
**请求体**:
```typescript
{
  text: string (必需) - 要转换为语音的文本（最大 50,000 字符）
  voice_id?: string (可选) - 语音 ID
  speed?: number (可选, 默认: 1.0) - 语音速度，范围: 0.5-2.0
  vol?: number (可选, 默认: 1.0) - 语音音量，范围: 0-10
  pitch?: number (可选, 默认: 0) - 语音音调，范围: -12 到 12
  emotion?: "happy" | "sad" | "angry" | "fearful" | "disgusted" | "surprised" | "calm" | "fluent" (可选) - 情感
  language_boost?: string (可选) - 语言增强，例如: "auto", "Chinese", "English"
  audio_setting?: {
    audio_sample_rate?: 8000 | 16000 | 22050 | 24000 | 32000 | 44100 (可选, 默认: 32000)
    bitrate?: 32000 | 64000 | 128000 | 256000 (可选, 默认: 128000, 仅 mp3)
    format?: "mp3" | "pcm" | "flac" (可选, 默认: "mp3")
    channel?: number (可选, 默认: 1)
  }
  wait?: boolean (可选, 默认: false) - 是否等待结果生成完成再返回
}
```
**返回**:
```typescript
{
  ok: true,
  task_id: string,
  status: string,
  outputs?: string[],  // 仅在 wait=true 时存在
  urls: {
    get: string  // 获取任务结果的 URL
  },
  tts_usage: {
    free_characters_used: number,  // 本次使用的免费字符数
    paid_characters: number,       // 本次需要付费的字符数
    credits_deducted: number,      // 本次扣除的积分数
    remaining_free: number,        // 今日剩余免费字符数
    total_used_today: number       // 今日总使用字符数
  }
}
// 错误情况：
{
  ok: false,
  code: 'UNAUTHORIZED' | 'MISSING_TEXT' | 'TEXT_TOO_LONG' | 'INVALID_SPEED' | 'INVALID_VOLUME' | 'INVALID_PITCH' | 'INVALID_EMOTION' | 'INSUFFICIENT_CREDITS' | 'CREDIT_DEDUCTION_ERROR' | 'WAVESPEED_ERROR' | 'INTERNAL_ERROR',
  message: string,
  required_credits?: number,  // 当 code='INSUFFICIENT_CREDITS' 时存在
  available_credits?: number  // 当 code='INSUFFICIENT_CREDITS' 时存在
}
```
**流程**:
1. 从 cookie 验证用户身份（使用 `createServerSupabaseClientForRouteHandler`）
2. 验证必需字段（text）
3. 验证可选参数范围（speed, vol, pitch, emotion）
4. **调用 `deduct_tts_credits` RPC 函数扣除积分**：
   - 每日免费额度：3,000 字符
   - 超出后：每 1,000 字符扣 2 积分
   - 如果积分不足，返回 402 错误
5. 调用 WaveSpeedAI API 提交文本转语音任务（模型: minimax-speech-02-hd）
6. 返回任务 ID、状态和使用情况

**认证方式**: 使用 cookie-based 认证，从 `NextRequest` 读取用户 session

**积分扣除规则**:
- 每日免费额度：3,000 字符（UTC 时间每天重置）
- 超出免费额度：每 1,000 字符扣 2 积分（向上取整）
- 积分不足时返回 402 Payment Required 错误

**环境变量**: `WAVESPEED_KEY`

**参考文档**: https://wavespeed.ai/docs/docs-api/minimax/minimax-speech-02-hd

#### GET `/api/speech/usage`
**功能**: 获取用户当前的 TTS 使用情况（免费额度）  
**请求方式**: `GET`  
**返回**:
```typescript
{
  ok: true,
  usage: {
    characters_used: number,      // 今日已使用的字符数
    remaining_free: number,        // 今日剩余免费字符数
    daily_limit: number            // 每日免费限额（3000字符）
  }
}
// 错误情况：
{
  ok: false,
  code: 'UNAUTHORIZED' | 'USAGE_FETCH_ERROR' | 'INTERNAL_ERROR',
  message: string
}
```
**流程**:
1. 从 cookie 验证用户身份（使用 `createServerSupabaseClientForRouteHandler`）
2. 查询 `tts_usage` 表获取今日使用量
3. 计算剩余免费额度（每日 3,000 字符）
4. 返回使用情况

**认证方式**: 使用 cookie-based 认证，从 `NextRequest` 读取用户 session

#### GET `/api/speech/result`
**功能**: 查询 TTS 任务结果  
**查询参数**:
```typescript
{
  task_id: string (必需) - 任务 ID
}
```
**返回**:
```typescript
{
  ok: true,
  task_id: string,
  status: string,
  outputs?: string[]  // 完成时包含音频 URL 数组
}
// 错误情况：
{
  ok: false,
  code: 'UNAUTHORIZED' | 'MISSING_TASK_ID' | 'TASK_NOT_FOUND' | 'WAVESPEED_ERROR' | 'INTERNAL_ERROR',
  message: string
}
```
**流程**:
1. 从 cookie 验证用户身份（使用 `createServerSupabaseClientForRouteHandler`）
2. 从 WaveSpeedAI API 获取任务状态
3. 返回任务状态和结果

**认证方式**: 使用 cookie-based 认证，从 `NextRequest` 读取用户 session

**环境变量**: `WAVESPEED_KEY`

#### GET `/api/proxy-file`
**功能**: 作为受限的文件代理，解决从 `cdn.infinitetalkai.org` 加载图片/音频时的 CORS 问题  
**请求方式**: `GET`  
**查询参数**:
```typescript
{
  url: string  // 必需，要代理的完整文件 URL，仅支持 https://cdn.infinitetalkai.org 域名
}
```
**返回**: 透传上游的二进制响应（图片或音频流），带关键头部：
`content-type`, `content-length`, `accept-ranges`, `content-range`, `cache-control`, `etag`, `last-modified`  
错误情况（JSON）：
```typescript
{
  ok: false,
  code: 'MISSING_URL' | 'INVALID_URL' | 'UNSUPPORTED_PROTOCOL' | 'HOST_NOT_ALLOWED',
  message: string
}
```
**流程**:
1. 从 query 中读取 `url` 参数
2. 校验 URL 协议为 `http/https`
3. 校验 `hostname === 'cdn.infinitetalkai.org'`，否则拒绝（防止变成通用代理）
4. 将请求转发到上游 CDN，并透传关键响应头与响应体
5. 不添加 CORS 头，由于同源（`/api`），前端可直接 `fetch` 获取 blob

**认证方式**: 无（仅限代理固定白名单域名，内部使用）

#### DELETE `/api/video/delete`
**功能**: 删除用户视频生成任务（作品）  
**查询参数**:
```typescript
{
  task_id: string (uuid, 必需) - 任务 ID
}
```
**返回**:
```typescript
{
  ok: true,
  message: "Task deleted successfully"
}
// 错误情况：
{
  ok: false,
  code: 'UNAUTHORIZED' | 'MISSING_TASK_ID' | 'TASK_NOT_FOUND' | 'DELETE_ERROR' | 'INTERNAL_ERROR',
  message: string
}
```
**流程**:
1. 从 cookie 验证用户身份（使用 `createServerSupabaseClientForRouteHandler`）
2. 验证任务归属（用户只能删除自己的任务，使用 cookie-based 客户端通过 RLS 检查）
3. **使用服务端密钥客户端（`getServerSupabase()`）执行删除操作，绕过 RLS 限制**
4. 验证删除结果，确保删除成功
5. 返回删除成功信息

**认证方式**: 使用 cookie-based 认证，从 `NextRequest` 读取用户 session

**安全**: 
- 双重验证（先用 cookie-based 客户端验证任务存在且属于当前用户）
- 使用服务端密钥绕过 RLS 执行删除（在已验证权限后）
- 删除操作仍包含 `user_id` 条件，防止误删

## 安全
- 禁止把 `SERVICE_ROLE` 放到任何客户端可达代码/环境变量。
- 对外返回数据**按需**选择字段；永远不要 `select *` 暴露敏感列。
- 限流：如有外部可调用接口，至少按 IP/会话做简单限流。

## 数据库表结构

⚠️ **重要维护提醒**：
- 🔴 当数据库有任何改动时，必须立即更新此文件
- 🔴 保持此表结构与实际数据库同步是强制要求
- 🔴 新增表、修改字段、添加约束时，添加相应的说明注释

### 📊 当前表结构 (2025-01-27)

#### 👤 user_info 表
```sql
- user_id (uuid, 主键) → 关联 auth.users.id
- credits (bigint, 默认: 0) → 用户积分
- created_at (timestamptz, 默认: now())
- updated_at (timestamptz, 默认: now())
```
**用途**: 存储用户积分信息
**RLS**: ✅ 启用
**新用户初始化**: 通过 `auth.users` 表的触发器 `on_auth_user_created` 自动初始化，用户注册后立即获得 10 积分欢迎奖励

#### 🛍️ products 表
```sql
- id (uuid, 主键, 默认: uuid_generate_v4())
- title (text) → 产品标题
- credits (integer, 约束: > 0) → 积分数量
- price_usd (numeric, 约束: >= 0) → 美元价格
- creem_product_id (text, 唯一) → Creem支付产品ID
- type (product_type枚举, 默认: 'one_time') → 产品类型
  - 枚举值: 'one_time', 'subscription'
- currency (text, 默认: 'USD') → 货币
- active (boolean, 默认: true) → 是否激活
- created_at (timestamptz, 默认: now())
```
**用途**: 存储产品/套餐信息
**RLS**: ✅ 启用

#### 💰 credit_ledger 表
```sql
- id (uuid, 主键, 默认: uuid_generate_v4())
- user_id (uuid) → 关联 auth.users.id
- delta (bigint) → 积分变动量（正数增加，负数减少）
- note (text, 可空) → 变动说明
- task_id (uuid, 可空) → 关联 video_tasks.id（视频任务扣分）
- created_at (timestamptz, 默认: now())
```
**用途**: 积分变动记录（审计日志）
**RLS**: ✅ 启用

#### 🎥 video_tasks 表
```sql
- id (uuid, 主键, 默认: uuid_generate_v4())
- user_id (uuid) → 关联 auth.users.id
- wavespeed_task_id (text, 唯一) → InfiniteTalk API 任务ID
- status (task_status枚举, 默认: 'pending') → 任务状态
  - 枚举值: 'pending', 'processing', 'completed', 'failed'
- resolution (text, 约束: 'fast'|'480p'|'720p') → 视频分辨率/模式
- credits_used (bigint, 约束: > 0) → 扣除的积分数
- video_duration_seconds (integer, 可空) → 生成的视频时长（秒）
- input_image_url (text) → 输入图片URL
- input_audio_url (text) → 输入音频URL
- output_video_url (text, 可空) → 输出视频URL
- prompt (text, 可空) → 用户提示词
- seed (integer, 可空) → 随机种子
- error_message (text, 可空) → 错误信息
- api_response (jsonb, 可空) → API原始响应
- expires_at (timestamptz, 可空) → 过期时间（任务完成时自动设置为 created_at + 7天，用于作品保留期）
- created_at (timestamptz, 默认: now())
- updated_at (timestamptz, 默认: now())
```
**用途**: 存储视频生成任务信息（包含用户作品）
**RLS**: ✅ 启用
**作品保留**: 任务完成时自动设置 `expires_at`，7天后过期（查询时过滤过期记录）

#### 📁 uploaded_files 表
```sql
- id (uuid, 主键, 默认: uuid_generate_v4())
- user_id (uuid) → 关联 auth.users.id
- task_id (uuid, 可空) → 关联 video_tasks.id（如果用于任务）
- file_type (file_type枚举) → 文件类型
  - 枚举值: 'image', 'audio', 'video'
- storage_path (text) → Supabase Storage路径
- storage_url (text) → 可公开访问的URL
- file_size (bigint, 约束: > 0) → 文件大小（字节）
- mime_type (text) → MIME类型
- created_at (timestamptz, 默认: now())
```
**用途**: 存储用户上传的文件元数据
**RLS**: ✅ 启用

#### 🔄 webhook_events 表
```sql
- id (uuid, 主键, 默认: uuid_generate_v4())
- webhook_id (text, 唯一, 非空) → WaveSpeed Webhook ID
- task_id (uuid, 关联 video_tasks.id)
- event_type (text) → 事件类型 (e.g. 'completed', 'failed')
- payload_json (jsonb) → 完整 payload
- created_at (timestamptz, 默认: now())
```
**用途**: 记录 Webhook 事件，用于幂等性检查和审计
**RLS**: ❌ 仅服务端使用（Service Role）

#### 📦 orders 表
```sql
- id (uuid, 主键, 默认: uuid_generate_v4())
- user_id (uuid) → 关联 auth.users.id
- product_id (uuid) → 关联 products.id
- units (integer, 默认: 1, 约束: > 0) → 购买数量
- status (order_status枚举, 默认: 'pending') → 订单状态
  - 枚举值: 'pending', 'paid', 'refunded', 'canceled'
- request_id (text) → 请求ID
- creem_session_id (text, 可空) → Creem会话ID
- creem_order_id (text, 可空) → Creem订单ID
- amount_total_cents (integer, 可空) → 总金额（分）
- currency (text, 可空, 默认: 'USD') → 货币
- created_at (timestamptz, 默认: now())
- paid_at (timestamptz, 可空) → 支付时间
```
**用途**: 订单管理
**RLS**: ✅ 启用

#### 💳 payments 表
```sql
- id (uuid, 主键, 默认: uuid_generate_v4())
- order_id (uuid, 可空) → 关联 orders.id
- provider (text, 默认: 'creem') → 支付提供商
- event_type (text) → 事件类型
- unique_key (text) → 唯一标识
- payload_json (jsonb) → 支付数据（JSON格式）
- created_at (timestamptz, 默认: now())
```
**用途**: 支付事件记录
**RLS**: ✅ 启用

#### 📝 tts_usage 表
```sql
- id (uuid, 主键, 默认: uuid_generate_v4())
- user_id (uuid) → 关联 auth.users.id
- usage_date (date, 默认: CURRENT_DATE) → 使用日期
- characters_used (integer, 默认: 0) → 当日使用的字符数
- created_at (timestamptz, 默认: now())
- updated_at (timestamptz, 默认: now())
- UNIQUE(user_id, usage_date) → 每个用户每天一条记录
```
**用途**: 记录用户每日 TTS 字符使用量（用于免费额度追踪）
**RLS**: ✅ 启用（用户只能查看自己的使用记录）

### 🔗 表关系图
```
auth.users (Supabase Auth)
    ↓
user_info (用户积分)
    ↓
credit_ledger (积分变动记录) → video_tasks (视频任务)
                                    ↓
                            uploaded_files (上传文件)

products (产品表)
    ↓
orders (订单表) → payments (支付记录)
```

### 📈 数据统计
- **总表数**: 9个 (新增 tts_usage)
- **有数据的表**: products (3行)
- **空表**: user_info, credit_ledger, orders, payments, video_tasks, uploaded_files, webhook_events, tts_usage
- **RLS状态**: video_tasks 需开启 Realtime Publication，且必须有 SELECT policy 允许用户读取自己的数据以便前端订阅。

## 触发器与函数（必须保持同步维护）

### credit_ledger ⇒ user_info 同步触发器
```sql
-- 名称：trg_credit_ledger_sync  表：public.credit_ledger  时机：AFTER INSERT/UPDATE/DELETE
-- 函数：public.tg_sync_user_credits()
-- 语义：仅以 ledger 为单一事实来源；任何对 ledger 的增删改都会同步到 user_info.credits
--      INSERT  → user_info.credits += NEW.delta
--      UPDATE  → user_info.credits = credits - OLD.delta + NEW.delta
--      DELETE  → user_info.credits -= OLD.delta
```

### user_info 更新触发器
```sql
-- 名称：trg_user_info_touch  表：public.user_info  时机：BEFORE UPDATE
-- 函数：public.tg_user_info_touch_updated_at()
-- 语义：写入 NEW.updated_at = now()
```

### RPC：public.add_user_credits
```sql
-- 参数：p_user_id uuid, p_credits_to_add bigint, p_note text
-- 权限：SECURITY DEFINER（仅服务端调用）
-- 行为（已修订，避免重复加分）：
--   1) 确保 user_info 行存在（不修改 credits）
--   2) 仅向 credit_ledger 写入一条 +delta 的记录
--      余额变动由触发器 tg_sync_user_credits() 自动完成
```

### RPC：public.create_video_task
```sql
-- 参数：p_user_id uuid, p_resolution text, p_input_image_url text, p_input_audio_url text, 
--       p_audio_duration_seconds integer, p_prompt text, p_seed integer
-- 权限：SECURITY DEFINER（仅服务端调用）
-- 行为：
--   1) 接收后端计算的音频时长（p_audio_duration_seconds，由 API 路由通过 music-metadata 库计算）
--   2) 计算所需积分：
--      - fast: 0.5积分/秒（向上取整），最小3积分
--      - 480p: 1积分/秒，最小5积分
--      - 720p: 2积分/秒，最小10积分
--      - 最大时长: 600秒
--   3) 检查用户积分是否足够
--   4) 创建任务记录（status = 'pending'）
--   5) 扣除积分（写入 credit_ledger，关联 task_id）
--   6) 返回任务 ID
-- 注意：音频时长由后端 API 路由计算，前端不再传递 audio_duration 字段
```

### RPC：public.update_video_task
```sql
-- 参数：p_task_id uuid, p_status task_status, p_wavespeed_task_id text, 
--       p_output_video_url text, p_video_duration_seconds integer, 
--       p_error_message text, p_api_response jsonb
-- 权限：SECURITY DEFINER（仅服务端调用）
-- 行为：更新任务状态、结果、错误信息等
```

### video_tasks 更新触发器
```sql
-- 名称：trg_video_tasks_updated_at  表：public.video_tasks  时机：BEFORE UPDATE
-- 函数：public.tg_update_updated_at()
-- 语义：写入 NEW.updated_at = now()
```

### tts_usage 更新触发器
```sql
-- 名称：trg_tts_usage_updated_at  表：public.tts_usage  时机：BEFORE UPDATE
-- 函数：public.tg_tts_usage_touch_updated_at()
-- 语义：写入 NEW.updated_at = now()
```

### RPC：public.initialize_user_with_welcome_credits
```sql
-- 参数：p_user_id uuid
-- 权限：SECURITY DEFINER（仅服务端调用）
-- 返回：jsonb
-- 行为：
--   1) 检查 user_info 是否已存在（幂等性检查）
--   2) 如果已存在，直接返回成功（不重复赠送）
--   3) 如果不存在：
--      - 创建 user_info 记录（credits = 0）
--      - 通过 credit_ledger 添加欢迎积分（10 积分）
--      - 触发器自动同步到 user_info.credits
--   4) 返回初始化结果（success, already_initialized, credits_added）
-- 返回格式：
--   {
--     success: boolean,
--     already_initialized: boolean,
--     credits_added?: integer (仅当 already_initialized=false 时存在),
--     message: string
--   }
-- 用途：新用户注册时自动初始化并赠送 10 积分欢迎奖励
```

### auth.users 触发器：自动初始化新用户
```sql
-- 名称：on_auth_user_created  表：auth.users  时机：AFTER INSERT
-- 函数：public.handle_new_user()
-- 语义：当用户通过 Supabase Auth 注册时，自动调用 initialize_user_with_welcome_credits
--       确保新用户注册后立即获得 10 积分欢迎奖励
-- 流程：
--   1) 用户通过 Supabase Auth 注册（Email OTP 或 Google OAuth）
--   2) auth.users 表插入新记录
--   3) 触发器自动执行 handle_new_user()
--   4) 调用 initialize_user_with_welcome_credits 初始化用户并赠送 10 积分
--   5) 用户注册完成后即可在个人中心看到 10 积分
```

### RPC：public.deduct_tts_credits
```sql
-- 参数：p_user_id uuid, p_characters integer
-- 权限：SECURITY DEFINER（仅服务端调用）
-- 返回：jsonb
-- 行为：
--   1) 检查用户今日 TTS 使用量（从 tts_usage 表）
--   2) 计算免费额度剩余（每日 3,000 字符）
--   3) 如果超出免费额度，计算需要扣除的积分（每 1,000 字符 2 积分，向上取整）
--   4) 检查用户积分是否足够
--   5) 扣除积分（写入 credit_ledger，note 包含字符数）
--   6) 更新 tts_usage 表的使用量
--   7) 返回使用情况（免费字符数、付费字符数、扣除积分、剩余免费额度等）
-- 返回格式：
--   {
--     success: boolean,
--     free_characters_used: integer,
--     paid_characters: integer,
--     credits_deducted: integer,
--     remaining_free: integer,
--     total_used_today: integer
--   }
--   或错误：
--   {
--     success: false,
--     error: 'INSUFFICIENT_CREDITS',
--     message: string,
--     required_credits: integer,
--     available_credits: integer
--   }
```

## Webhook 幂等与订单映射
- 幂等锁：优先插入 payments(unique_key = event.id) 作为“预占位”。若唯一键冲突则直接返回 200，不再加分。
- 次级幂等：若 `orders.creem_session_id = checkout.id` 已存在，则仅更新既有 payments 的 `order_id/payload_json`，不重复计费。
- 产品映射：用 `checkout.order.product` 或 `checkout.product.id` 匹配 `products.creem_product_id`，得到本地 `products.id` 作为 `orders.product_id`。